# support advice: autodo-kit -> kit-dependency-governance

## Context

`autodo-kit` supports `kit-dependency-governance` because `kit-dependency-governance` depends on `autodo-kit`.

## Adaptation Advice

Describe what `kit-dependency-governance` should update if `autodo-kit` changes.

## Suggested Steps

- Required change:
- Compatibility note:
- Validation command:
- Deadline or release note:
