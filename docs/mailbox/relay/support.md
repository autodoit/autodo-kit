# mailbox/relay support: autodo-kit

This project supports the following governed local projects.

- `academic-research-kit`: write adaptation advice in `updates/outbox/academic-research-kit/`.
- `autodo-suite`: write adaptation advice in `updates/outbox/autodo-suite/`.
