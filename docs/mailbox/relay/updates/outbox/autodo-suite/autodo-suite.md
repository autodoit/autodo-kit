# support advice (out-down): autodo-kit -> autodo-suite

## Context

`autodo-kit` supports `autodo-suite` because `autodo-suite` depends on `autodo-kit`.

## Adaptation Advice

Describe what `autodo-suite` should update if `autodo-kit` changes.

## Suggested Steps

- Required change:
- Compatibility note:
- Validation command:
- Deadline or release note:
